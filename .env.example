# Your WhatsApp number in international format without + (e.g. 23057123456)
PUBLIC_WHATSAPP_NUMBER=

# Contact email — where form submissions are sent (added when Worker is built)
CONTACT_EMAIL=

# Resend API key (added when Worker is built)
RESEND_API_KEY=
